<%- include('../partials/header', { title: title }) %>

<h2>Kirim Notifikasi ke Pengguna</h2>
<p>Kirim pesan, update, atau promo langsung ke lonceng notifikasi pengguna.</p>

<div class="card">
    <form action="/admin/notifications/send" method="POST">
        <div class="form-group">
            <label for="userId">Kirim Ke:</label>
            <select name="userId" id="userId" class="form-control" required>
                <option value="">-- Pilih Pengguna --</option>
                <% users.forEach(user => { %>
                    <option value="<%= user._id %>"><%= user.name %> (<%= user.email %>)</option>
                <% }) %>
            </select>
        </div>
        <div class="form-group">
            <label for="title">Judul Notifikasi</label>
            <input type="text" name="title" id="title" class="form-control" required>
        </div>
        <div class="form-group">
            <label for="message">Pesan Singkat</label>
            <textarea name="message" id="message" class="form-control" required></textarea>
        </div>
        <div class="form-group">
            <label for="link">Link Tujuan (saat diklik)</label>
            <input type="text" name="link" id="link" class="form-control" placeholder="Contoh: /dashboard/settings" required>
        </div>
        <button type="submit" class="btn btn-primary">Kirim Notifikasi</button>
    </form>
</div>

<%- include('../partials/footer') %>