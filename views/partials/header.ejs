<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'DigitalHostID' %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --sidebar-bg: #1f1d2b;
            --main-bg: #f4f7f9;
            --primary-accent: #82aaff;
            --secondary-accent: #8e44ad;
            --text-light: #f9fafb;
            --text-dark: #1f2937;
            --border-color: #3e3c4a;
            --notif-red: #ef4444;
            --card-bg: #ffffff;
            --shadow-light: 0 4px 6px rgba(0,0,0,0.05);
            --shadow-medium: 0 8px 15px rgba(0,0,0,0.1);
            --border-radius-sm: 8px;
            --border-radius-lg: 12px;
            --button-hover-transform: translateY(-2px);
        }

        *, *::before, *::after {
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            background-color: var(--main-bg);
            color: var(--text-dark);
            line-height: 1.6;
        }

        h1, h2, h3, h4, h5, h6 {
            font-weight: 700;
            color: var(--text-dark);
        }

        .container {
            max-width: 1140px;
            margin: 0 auto;
            padding: 0 15px;
        }

        .sidebar {
            height: 100%;
            width: 260px;
            position: fixed;
            z-index: 1001;
            top: 0;
            left: 0;
            background-color: var(--sidebar-bg);
            padding-top: 15px;
            transform: translateX(-100%);
            transition: transform 0.3s ease;
            box-shadow: 5px 0 15px rgba(0,0,0,0.2);
            display: flex;
            flex-direction: column;
        }

        .sidebar.open {
            transform: translateX(0);
        }

        #sidebar-close {
            position: absolute;
            top: 15px;
            right: 20px;
            font-size: 1.5rem;
            color: var(--text-light);
            cursor: pointer;
            z-index: 1002;
        }

        .sidebar-header {
            padding: 0 25px 15px 25px;
            text-align: center;
            border-bottom: 1px solid var(--border-color);
        }

        .sidebar-header a {
            color: var(--text-light);
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .sidebar-header img {
            max-height: 40px;
            border-radius: 50%;
        }

        .sidebar-header span {
            font-size: 1.4rem;
            font-weight: 700;
        }

        .sidebar-nav {
            list-style: none;
            padding: 20px 0;
            margin: 0;
            flex-grow: 1;
        }

        .sidebar-nav li a {
            display: flex;
            align-items: center;
            padding: 15px 25px;
            color: var(--text-light);
            text-decoration: none;
            transition: all 0.2s;
            font-size: 1.05rem;
        }

        .sidebar-nav li a:hover,
        .sidebar-nav li a.active {
            background-color: var(--primary-accent);
            color: var(--sidebar-bg);
        }

        .sidebar-nav li a i {
            width: 30px;
            margin-right: 15px;
            font-size: 1.2rem;
            text-align: center;
        }

        .sidebar-footer {
            padding: 20px 25px;
            border-top: 1px solid var(--border-color);
        }

        .sidebar-footer a {
            color: #aaa;
            text-decoration: none;
            display: flex;
            align-items: center;
            transition: color 0.2s;
        }

        .sidebar-footer a:hover {
            color: white;
        }

        .content-wrapper {
            margin-left: 0;
            transition: margin-left 0.3s ease;
        }

        @media (min-width: 992px) {
            .sidebar {
                transform: translateX(0);
                box-shadow: none;
            }
            .content-wrapper {
                margin-left: 260px;
            }
            #sidebar-toggle, #sidebar-close {
                display: none;
            }
        }

        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #fff;
            padding: 15px 25px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
            position: sticky;
            top: 0;
            z-index: 500;
        }

        #sidebar-toggle {
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-dark);
        }

        .top-bar-right {
            position: relative;
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .user-profile {
            font-weight: 500;
            color: var(--text-dark);
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
            position: relative;
        }

        .user-profile img {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            object-fit: cover;
        }

        .user-dropdown {
            display: none;
            position: absolute;
            top: 100%;
            right: 0;
            background: var(--card-bg);
            border-radius: var(--border-radius-sm);
            box-shadow: var(--shadow-medium);
            z-index: 1003;
            margin-top: 10px;
            min-width: 180px;
        }

        .user-dropdown.show {
            display: block;
        }

        .user-dropdown a {
            display: block;
            padding: 10px 15px;
            color: var(--text-dark);
            text-decoration: none;
            transition: background-color 0.2s;
        }

        .user-dropdown a:hover {
            background-color: var(--main-bg);
        }

        .notification-bell {
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-dark);
            position: relative;
        }

        .notification-dot {
            position: absolute;
            top: -5px;
            right: -5px;
            width: 10px;
            height: 10px;
            background-color: var(--notif-red);
            border-radius: 50%;
            border: 2px solid white;
        }

        .notification-dropdown {
            display: none;
            position: absolute;
            top: 100%;
            right: 0;
            width: 320px;
            background: var(--card-bg);
            border-radius: var(--border-radius-sm);
            box-shadow: var(--shadow-medium);
            z-index: 1002;
            margin-top: 10px;
            max-height: 400px;
            overflow-y: auto;
        }

        .notification-dropdown.show {
            display: block;
        }

        .notif-header {
            font-weight: 600;
            padding: 12px 15px;
            border-bottom: 1px solid #eee;
        }

        .notif-item {
            padding: 12px 15px;
            border-bottom: 1px solid #eee;
            transition: background-color 0.2s;
        }

        .notif-item:last-child {
            border-bottom: none;
        }

        .notif-item:hover {
            background-color: var(--main-bg);
        }

        .notif-item a {
            text-decoration: none;
            color: inherit;
            display: block;
        }

        .notif-item h4 {
            margin: 0 0 5px 0;
            font-size: 0.95rem;
            color: var(--text-dark);
        }

        .notif-item p {
            margin: 0;
            font-size: 0.85rem;
            color: #666;
        }

        .main-content {
            padding: 2rem;
            flex-grow: 1;
        }

        .card {
            background-color: var(--card-bg);
            border-radius: var(--border-radius-lg);
            padding: 1.5rem;
            box-shadow: var(--shadow-light);
            margin-bottom: 1.5rem;
        }

        .public-content-wrapper {
            padding: 2rem;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            flex-direction: column;
        }

        .alert {
            padding: 1rem;
            border-radius: var(--border-radius-sm);
            margin-bottom: 1.5rem;
            border: 1px solid transparent;
        }

        .alert-success {
            background-color: #d4edda;
            color: #155724;
            border-color: #c3e6cb;
        }

        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
            border-color: #f5c6cb;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 12px 28px;
            border: none;
            border-radius: 50px;
            font-weight: 600;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.95rem;
            box-shadow: var(--shadow-light);
        }

        .btn-primary {
            background: linear-gradient(45deg, var(--primary-accent), #577ed4);
            color: white;
        }

        .btn-primary:hover {
            transform: var(--button-hover-transform);
            box-shadow: 0 8px 20px rgba(130, 170, 255, 0.4);
        }

        .btn-secondary {
            background: linear-gradient(45deg, var(--secondary-accent), #6c3483);
            color: white;
        }

        .btn-secondary:hover {
            transform: var(--button-hover-transform);
            box-shadow: 0 8px 20px rgba(142, 68, 173, 0.4);
        }

        .btn-danger {
            background-color: #ef4444;
            color: white;
        }

        .btn-danger:hover {
            transform: var(--button-hover-transform);
            background-color: #dc2626;
        }

        .form-group {
            margin-bottom: 1rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #333;
        }

        .form-control {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #e5e7eb;
            border-radius: var(--border-radius-sm);
            font-family: 'Poppins', sans-serif;
            font-size: 1rem;
            transition: border-color 0.2s, box-shadow 0.2s;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary-accent);
            box-shadow: 0 0 0 3px rgba(130, 170, 255, 0.2);
        }

        .form-control[readonly] {
            background-color: #f3f4f6;
            cursor: not-allowed;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
        }

        table th, table td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #eee;
        }

        table th {
            border-bottom-width: 2px;
            font-weight: 600;
            background-color: #f9fafb;
        }
    </style>
</head>
<body>

<% if (user) { %>
    <aside class="sidebar" id="sidebar">
        <i class="fas fa-times" id="sidebar-close"></i>
        <div class="sidebar-header">
            <a href="/">
                <img src="https://files.catbox.moe/sepzlr.png" alt="Logo">
                <span>DigitalHostID</span>
            </a>
        </div>
        <ul class="sidebar-nav">
            <li><a href="/dashboard" class="<%= currentPath === '/dashboard' ? 'active' : '' %>"><i class="fas fa-tachometer-alt"></i> Dashboard</a></li>
            <li><a href="/dashboard/buy-domain" class="<%= currentPath === '/dashboard/buy-domain' ? 'active' : '' %>"><i class="fas fa-plus-circle"></i> Beli Domain</a></li>
            <li><a href="/dashboard/transfer-domain" class="<%= currentPath === '/dashboard/transfer-domain' ? 'active' : '' %>"><i class="fas fa-exchange-alt"></i> Transfer Domain</a></li>
            <li><a href="/dashboard/settings" class="<%= currentPath === '/dashboard/settings' ? 'active' : '' %>"><i class="fas fa-user-cog"></i> Pengaturan</a></li>
            <% if (user.role === 'admin') { %>
                <li><a href="/admin" class="<%= currentPath.startsWith('/admin') ? 'active' : '' %>"><i class="fas fa-shield-alt"></i> Panel Admin</a></li>
            <% } %>
        </ul>
        <div class="sidebar-footer">
            <a href="/logout"><i class="fas fa-sign-out-alt" style="margin-right: 15px;"></i> Logout</a>
        </div>
    </aside>

    <div class="content-wrapper">
        <header class="top-bar">
            <i class="fas fa-bars" id="sidebar-toggle"></i>
            <div class="top-bar-right">
                <div class="notification-bell" id="notification-bell">
                    <i class="fas fa-bell"></i>
                    <% if (notifications && notifications.length > 0) { %><div class="notification-dot"></div><% } %>
                </div>
                <div class="notification-dropdown" id="notification-dropdown">
                    <div class="notif-header">Notifikasi</div>
                    <% if (notifications && notifications.length > 0) { %>
                        <% notifications.forEach(notif => { %>
                        <a href="<%= notif.link %>">
                            <div class="notif-item"><h4><%= notif.title %></h4><p><%= notif.message %></p></div>
                        </a>
                        <% }) %>
                    <% } else { %>
                        <div class="notif-item"><p>Tidak ada notifikasi baru.</p></div>
                    <% } %>
                </div>

                <div class="user-profile" id="user-profile-toggle">
                    <img src="/images/default-avatar.png" alt="Avatar">
                    <span>Halo, <%= user.name %></span>
                    <i class="fas fa-caret-down"></i>
                </div>
                <div class="user-dropdown" id="user-dropdown">
                    <a href="/dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                    <a href="/dashboard/settings"><i class="fas fa-user-cog"></i> Pengaturan Akun</a>
                    <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                </div>
            </div>
        </header>
        
        <div style="padding: 0 2rem;">
            <% if (success_msg && success_msg.length > 0) { %><div class="alert alert-success" style="margin-top: 1rem;"><%= success_msg[0] %></div><% } %>
            <% if (error_msg && error_msg.length > 0) { %><div class="alert alert-danger" style="margin-top: 1rem;"><%= error_msg[0] %></div><% } %>
        </div>

        <main class="main-content">
<% } else { %>
    <div class="public-content-wrapper">
        <main>
<% } %>