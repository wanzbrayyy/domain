<%- include('../partials/header', { title: 'Dashboard' }) %>

<div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; margin-bottom: 1.5rem;">
    <h1>Selamat Datang, <%= user.name %>!</h1>
    <div style="display: flex; gap: 1rem;">
        <a href="/dashboard/transfer-domain" class="btn btn-secondary">
            <i class="fas fa-exchange-alt"></i> Transfer Domain
        </a>
        <a href="/dashboard/buy-domain" class="btn btn-primary">
            <i class="fas fa-plus"></i> Beli Domain Baru
        </a>
    </div>
</div>

<div class="card">
    <h2><i class="fas fa-globe" style="margin-right: 10px; color: var(--nexus-primary);"></i>Domain Anda</h2>
    
    <div class="table-wrapper">
        <table>
            <thead>
                <tr>
                    <th>Nama Domain</th>
                    <th>Status</th>
                    <th>Kedaluwarsa</th>
                    <th>Aksi</th>
                </tr>
            </thead>
            <tbody>
                <% if (domains && domains.length > 0) { %>
                    <% domains.forEach(domain => { %>
                        <tr>
                            <td><%= domain.name %></td>
                            <td><%= domain.status_desc %></td>
                            <td><%= new Date(domain.expiry_date).toLocaleDateString('id-ID', { year: 'numeric', month: 'long', day: 'numeric' }) %></td>
                            <td>
                                <a href="/dashboard/domain/<%= domain.id %>/manage" title="Kelola Domain" style="margin-right: 15px; font-size: 1.2rem;">
                                    <i class="fas fa-cog"></i>
                                </a>
                                <a href="#" title="Perbarui Kontak (Coming Soon)" style="font-size: 1.2rem;">
                                    <i class="fas fa-address-book"></i>
                                </a>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="4" style="padding: 20px; text-align: center;">
                            Anda belum memiliki domain. Klik tombol di atas untuk mendaftarkan domain pertama Anda!
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</div>

<%- include('../partials/footer') %>