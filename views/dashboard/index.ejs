<%- include('../partials/header', { title: 'Dashboard', currentPath: '/dashboard' }) %>

<div class="d-flex justify-content-between align-items-center mb-4 flex-wrap">
    <h1>Selamat Datang, <%= user.name %>!</h1>
    <div class="d-flex gap-2">
        <a href="/dashboard/transfer-domain" class="btn btn-secondary">
            <i class="fas fa-exchange-alt" style="margin-right: 8px;"></i> Transfer Domain
        </a>
        <a href="/dashboard/buy-domain" class="btn btn-primary">
            <i class="fas fa-plus" style="margin-right: 8px;"></i> Beli Domain Baru
        </a>
    </div>
</div>

<div class="card">
    <h2><i class="fas fa-globe" style="margin-right: 10px; color: var(--primary-accent);"></i>Domain Anda</h2>
    <div style="overflow-x: auto;">
        <table>
            <thead>
                <tr>
                    <th>Nama Domain</th>
                    <th>Status</th>
                    <th>Kedaluwarsa</th>
                    <th>Aksi</th>
                </tr>
            </thead>
            <tbody>
                <% if (domains && domains.length > 0) { %>
                    <% domains.forEach(domain => { %>
                        <tr>
                            <td><%= domain.name %></td>
                            <td><%= domain.status_desc %></td>
                            <td><%= new Date(domain.expiry_date).toLocaleDateString('id-ID', { year: 'numeric', month: 'long', day: 'numeric' }) %></td>
                            <td>
                                <a href="/dashboard/domain/<%= domain.id %>/manage" title="Kelola Domain">
                                    <i class="fas fa-cog"></i>
                                </a>
                            </td>
                        </tr>
                    <% }) %>
                <% } else { %>
                    <tr>
                        <td colspan="4" style="padding: 20px; text-align: center;">
                            Anda belum memiliki domain. Klik tombol di atas untuk mendaftarkan domain pertama Anda!
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</div>

<%- include('../partials/footer') %>